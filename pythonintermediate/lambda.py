# This code defines a lambda function to calculate the cube of a number
# create the function
get_cube = lambda number: number**3    #lambda argument and expression

# take integer input
number = int(input())

# call the function and print the result
print(get_cube(number))      #call the function with the input number