# a function to check if a given string contains spacce or not
def has_spaces(s):
    """
    Check if the given string contains any spaces.

    Args:
        s (str): The string to check.

    Returns:
        bool: True if the string contains spaces, False otherwise.
    """
    return ' ' in s 